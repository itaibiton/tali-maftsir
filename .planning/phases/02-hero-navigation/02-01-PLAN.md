---
phase: 02-hero-navigation
plan: 01
type: execute
---

<objective>
Create a compelling Hero section with professional photo, Hebrew headline addressing visitor pain points, and WhatsApp CTA button, plus a sticky navigation header.

Purpose: Establish the primary conversion landing point — this is the first thing visitors see and must immediately communicate Tali's value and provide a clear path to action.
Output: Working Hero section with photo, headline, CTA button, and navigation header.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-01-SUMMARY.md

**Key files from Phase 1:**
@src/app/layout.tsx
@src/app/globals.css
@src/app/page.tsx

**Available assets:**
- /public/images/tali-portrait-1.jpg
- /public/images/tali-portrait-2.jpg

**Tech stack available:** Next.js 16, React 19, Tailwind CSS v4, TypeScript
**Established patterns:** CSS variables for colors, RTL-first layout, Heebo font

**Constraining decisions from Phase 1:**
- Tailwind v4 CSS-based theming via @theme directive
- Green palette: primary #8FBC8F, dark #5B8A5B, light #C1E1C1, accent #D4A574
- Heebo font with weights 400-700

**Design direction from PROJECT.md:**
- Luxurious: sophisticated, organized, mature
- Warm: humanity, good eyes, lightness
- Containing: experience, acceptance, solutions exist
- NOT: spiritual/new-age/huggy-touchy

**Target audience pain points (for headline inspiration):**
- "היחסים שלי עם... לא עובדים"
- "אני לא יכולה יותר להמשיך להרגיש לא טוב עם עצמי לגבי..."
- "אני רוצה לפרוץ ולא מצליח/לא יודע איך"
- "נפגעתי בעבר ואני לא יכול להמשיך ככה"
- "כנראה שיש בי יותר ממה שכרגע קורה בחיים שלי"
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Hero section with photo, headline, and WhatsApp CTA</name>
  <files>src/app/page.tsx</files>
  <action>
Replace the current test page content with a proper Hero section:

1. **Layout:** Full-width section, min-height of viewport, centered content with appropriate padding

2. **Photo placement:** Use tali-portrait-1.jpg with professional styling:
   - Larger than current test (consider 200-250px on mobile, 300-350px on desktop)
   - Soft shadow and subtle border treatment
   - Rounded or circular depending on what looks best with photo

3. **Headline:** Create compelling Hebrew headline that speaks to transformation, e.g.:
   - Main: "הזמן להפסיק להסתדר ולהתחיל לחיות"
   - Or similar that conveys change is possible without being spiritual/new-age

4. **Subheadline:** Short supporting text about Tali's approach:
   - "ליווי אישי בשיטת סאטיר | צמיחה מתוך חיבור"
   - Convey professional + warm combination

5. **WhatsApp CTA button:**
   - Text: "לשיחת היכרות בוואטסאפ" or "לתיאום שיחת היכרות"
   - Link: `https://wa.me/PHONE_NUMBER` (use placeholder +972XXXXXXXXX for now)
   - Styling: primary color background, white text, rounded, shadow, hover effect
   - Consider adding WhatsApp icon (use inline SVG or emoji 📱)

6. **Responsive:** Mobile-first approach
   - Stack vertically on mobile
   - Adjust text sizes for screens (text-2xl on mobile, text-4xl+ on desktop)
   - Appropriate spacing

Use CSS variables established in globals.css for colors. Keep test color swatches comment only (remove from visible UI).
  </action>
  <verify>Run `npm run dev` and visually inspect Hero section at localhost:3000</verify>
  <done>Hero displays with photo, headline, subheadline, and WhatsApp CTA. Looks professional and warm, not spiritual/hippie. RTL alignment correct.</done>
</task>

<task type="auto">
  <name>Task 2: Create sticky Navigation header</name>
  <files>src/app/components/Header.tsx, src/app/page.tsx</files>
  <action>
Create a Header component and integrate it:

1. **Create components directory:** `src/app/components/`

2. **Header.tsx component:**
   - Fixed/sticky at top with `position: sticky` or `fixed`
   - Semi-transparent background with backdrop blur for modern look
   - Contains:
     - Brand name "טלי מפציר" on the right (RTL)
     - Placeholder navigation links on the left: "אודות", "שירותים", "שאלות", "צור קשר"
     - Links are text only for now (will become anchor links in later phases)
   - Height: ~64-80px
   - Z-index appropriate for stacking

3. **Responsive behavior:**
   - Desktop: Show all nav links
   - Mobile: Either hide nav links (show only brand name) or implement simple hamburger
   - For v1 simplicity: hide nav links on mobile, just show brand name

4. **Styling:**
   - Clean, minimal design matching design direction
   - Use neutral colors for text, subtle divider or shadow at bottom
   - On scroll, optionally add shadow or change background opacity

5. **Integration:** Import Header in page.tsx and render above Hero section

Avoid over-engineering — simple sticky header is sufficient for this phase.
  </action>
  <verify>Run `npm run dev`, scroll page, confirm header stays at top. Check mobile viewport (use browser dev tools).</verify>
  <done>Header component exists, displays brand name and nav links (desktop), is sticky, looks clean and professional.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Hero section with professional photo, Hebrew headline, WhatsApp CTA button, and sticky navigation header</what-built>
  <how-to-verify>
    1. Run: `npm run dev`
    2. Visit: http://localhost:3000
    3. Desktop check:
       - Header visible at top with brand name and nav links
       - Hero section displays with photo, headline, subheadline, CTA
       - Scroll down — header stays sticky
       - Colors match green palette (sage green)
       - Typography is Heebo, RTL aligned
    4. Mobile check (DevTools → responsive mode → 375px width):
       - Hero content stacks vertically
       - Text sizes appropriate for mobile
       - CTA button easily tappable
       - Header shows brand name (nav links may be hidden)
    5. Confirm: Design feels professional + warm, NOT spiritual/hippie
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `npm run build` succeeds without errors
- [ ] `npm run lint` passes
- [ ] Hero section displays correctly on desktop and mobile
- [ ] Header is sticky and contains navigation
- [ ] WhatsApp CTA button present and styled
- [ ] RTL layout correct throughout
- [ ] Design matches professional + warm direction (not spiritual/hippie)
</verification>

<success_criteria>

- Hero section created with photo, headline, WhatsApp CTA
- Sticky navigation header implemented
- Mobile-responsive layout working
- Human verification checkpoint passed
- All verification checks pass
- No TypeScript or lint errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-hero-navigation/02-01-SUMMARY.md` following the summary template with:
- Accomplishments (Hero + Header created)
- Files created/modified
- Design decisions made (headline text, photo styling choices)
- Issues encountered (if any)
- Next step: Ready for Phase 3 (About & Services)
</output>
