---
phase: 06-polish-responsive
plan: 01
type: execute
---

<objective>
Add mobile navigation menu and finalize responsive polish.

Purpose: Complete the landing page with mobile-friendly navigation and address remaining polish items.
Output: Mobile hamburger menu, optimized images, verified responsive layout across breakpoints.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-hero-navigation/02-01-SUMMARY.md

# Key files:
@src/app/components/Header.tsx
@src/app/components/Hero.tsx

**Tech stack available:** Next.js 15, Tailwind v4 (CSS-based theming), Framer Motion
**Established patterns:**
- Framer Motion whileInView animations (opacity: 0→1, y: 30→0)
- AnimatePresence for enter/exit animations (from FAQSection)
- CSS variables for colors: --color-primary-dark, --color-neutral-50, --color-neutral-600
- Client components for interactivity ("use client")

**Current issues:**
- Header nav hidden on mobile (no hamburger menu)
- Hero uses `<img>` instead of `next/image` (lint warning)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add mobile hamburger menu to Header</name>
  <files>src/app/components/Header.tsx</files>
  <action>
Add mobile menu to Header:

1. Add useState for `isMenuOpen`
2. Add hamburger button (3 lines → X when open) visible only on mobile (`md:hidden`)
3. Add mobile menu drawer that slides down when open
4. Use Framer Motion AnimatePresence for smooth open/close animation
5. Include all nav links plus close button
6. Close menu when clicking a link or outside

Structure:
- Hamburger button: 3 horizontal lines, transitions to X
- Mobile menu: Full-width dropdown below header with nav links
- Animation: height 0→auto or translateY slide

Keep existing desktop nav unchanged.
  </action>
  <verify>Mobile view shows hamburger, clicking opens menu with nav links, clicking link closes menu</verify>
  <done>Header has working mobile menu with smooth animation, desktop nav unchanged</done>
</task>

<task type="auto">
  <name>Task 2: Convert Hero img to next/image and final polish</name>
  <files>src/app/components/Hero.tsx</files>
  <action>
1. Replace `<img>` with Next.js `<Image>` component:
   - Import Image from 'next/image'
   - Add width/height props (use actual dimensions or reasonable estimate like 400x400)
   - Keep priority={true} for above-fold image
   - Adjust styling as needed for Next.js Image behavior

2. Verify the gradient overlay still works with Image component

This resolves the ESLint warning: "@next/next/no-img-element"
  </action>
  <verify>npm run lint shows no img warnings, Hero image loads correctly with visible animation</verify>
  <done>Hero uses next/image, no lint warnings, image displays correctly</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `npm run build` succeeds without errors
- [ ] `npm run lint` passes with no errors (warnings acceptable)
- [ ] Mobile menu opens/closes smoothly
- [ ] All nav links work on mobile
- [ ] Hero image loads without layout shift
- [ ] All sections responsive at 375px, 768px, 1024px widths
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Mobile navigation fully functional
- No img-element lint warnings
- Landing page complete and polished
</success_criteria>

<output>
After completion, create `.planning/phases/06-polish-responsive/06-01-SUMMARY.md` following summary template.

This is the FINAL PHASE - milestone complete after this plan!
</output>
