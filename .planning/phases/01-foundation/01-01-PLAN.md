---
phase: 01-foundation
plan: 01
type: execute
---

<objective>
Set up Next.js project with Tailwind CSS, RTL support, Hebrew fonts, and green color palette.

Purpose: Establish the technical foundation that all landing page sections will build upon.
Output: Working Next.js app with proper Hebrew RTL layout, Heebo font, and green theme configured.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

**Design constraints from PROJECT.md:**
- Language: Hebrew only, full RTL support
- Tech Stack: Next.js + Tailwind CSS
- Color: Light green base with complementary tones
- Aesthetic: Luxurious, warm, containing — NOT spiritual/new-age

**Assets available:**
- IMG_3648.JPG, IMG_3649.JPG (professional photos in project root)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Next.js project with TypeScript and Tailwind CSS</name>
  <files>package.json, tsconfig.json, tailwind.config.ts, postcss.config.mjs, src/app/layout.tsx, src/app/page.tsx, src/app/globals.css, next.config.ts</files>
  <action>
    Run `npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir --import-alias "@/*"` with defaults.
    Accept overwrite prompts for existing files.
    Use App Router (src/app directory structure).
    After creation, verify the scaffold works with `npm run dev`.
  </action>
  <verify>npm run dev starts without errors, localhost:3000 shows Next.js welcome page</verify>
  <done>Next.js 15 project scaffold created with Tailwind CSS, TypeScript, and App Router</done>
</task>

<task type="auto">
  <name>Task 2: Configure RTL support and Hebrew fonts</name>
  <files>src/app/layout.tsx, src/app/globals.css</files>
  <action>
    1. In layout.tsx:
       - Set html lang="he" and dir="rtl"
       - Import Heebo font from next/font/google (weights: 400, 500, 600, 700)
       - Apply Heebo as the default font via className on body
       - Set metadata: title="טלי מפציר | ליווי אישי בשיטת סאטיר", description in Hebrew

    2. In globals.css:
       - Ensure Tailwind directives are present (@tailwind base/components/utilities)
       - Add base styles for RTL: text-right default, proper RTL spacing utilities
       - Set body background to a subtle off-white (not pure white)

    Do NOT add custom RTL utility classes — Tailwind v3+ has built-in RTL support via `rtl:` and `ltr:` variants.
  </action>
  <verify>npm run dev shows Hebrew metadata in browser tab, page flows right-to-left, Heebo font renders</verify>
  <done>RTL direction active, Hebrew lang set, Heebo font applied to all text</done>
</task>

<task type="auto">
  <name>Task 3: Set up green color palette in Tailwind config</name>
  <files>tailwind.config.ts, src/app/page.tsx</files>
  <action>
    1. In tailwind.config.ts, extend theme.colors with custom palette:
       - primary: Light green (#8FBC8F or similar sage green — conveys growth, calm)
       - primary-dark: Darker green for hover states
       - primary-light: Lighter green for backgrounds
       - accent: Warm complementary tone (soft gold or warm beige)
       - neutral: Clean white and neutral grays/beiges

    2. Replace the default page.tsx content with a minimal Hebrew test page:
       - White/off-white background
       - Centered heading "טלי מפציר" in primary-dark
       - Subheading "ליווי אישי בשיטת סאטיר" in neutral gray
       - A green CTA button placeholder with "לשיחת היכרות" text
       - Use the photo IMG_3648.JPG as a test image display (move to public/ first)

    This validates the color palette, fonts, and RTL all work together.

    Move photos to public/images/:
    - mv IMG_3648.JPG public/images/tali-portrait-1.jpg
    - mv IMG_3649.JPG public/images/tali-portrait-2.jpg
  </action>
  <verify>npm run dev shows test page with green palette, Hebrew text renders RTL, photo displays correctly</verify>
  <done>Green color palette configured, test page renders with correct colors/fonts/RTL, photos accessible in public/</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `npm run build` succeeds without errors
- [ ] `npm run dev` shows test page at localhost:3000
- [ ] Page renders in RTL direction (text flows right-to-left)
- [ ] Heebo Hebrew font is applied (check DevTools font-family)
- [ ] Green color palette classes work (bg-primary, text-primary-dark, etc.)
- [ ] Browser tab shows Hebrew title
- [ ] Photos accessible at /images/tali-portrait-1.jpg
</verification>

<success_criteria>

- All 3 tasks completed
- All verification checks pass
- No TypeScript or build errors
- Foundation ready for Phase 2 (Hero section)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md` following the summary template.
</output>
